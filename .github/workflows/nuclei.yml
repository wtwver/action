name: Scan Target

on:
   # schedule:
     # - cron: '0 0 * * *'
    # workflow_dispatch
    push:

jobs:
  nuclei-scan:
    runs-on: ubuntu-latest
    steps:
      - run: free -m
      - uses: actions/checkout@v2
      - run: wget https://raw.githubusercontent.com/wtwver/atm/main/config/reconftw.bbrf?token=AM2642AZ5QKGJWXDYJB4SYTBLAAVY -O reconftw.bbrf
      - run: wget https://raw.githubusercontent.com/wtwver/atm/main/config/notify.conf?token=AM2642AYJLAB7BRNM25DFDTBLAAUE -O notify.conf
      - run: ls -R
      # - run: docker run projectdiscovery/nuclei -u ${{ secrets.HTTP }}://${{ secrets.DOMAIN }}
      # - run: docker run -m 5g -v ${PWD}/notify.conf:/root/Tools/reconftw/notify.conf -v ${PWD}/reconftw.bbrf:/root/Tools/reconftw/reconftw.cfg wtwver/reconftw -d ${{ secrets.DOMAIN }} -s
      - run: cat ${PWD}/atm/config/notify.conf
      - run: docker run -m 5g -v -v ${PWD}/atm/config/notify.conf:/root/Tools/reconftw/notify.conf -v ${PWD}/atm/config/reconftw.bbrf:/root/Tools/reconftw/reconftw.cfg wtwver/reconftw -d testfire.net -s
name: Scan Target

on:
   # schedule:
     # - cron: '0 0 * * *'
    # workflow_dispatch
    push:

jobs:
  nuclei-scan:
    runs-on: ubuntu-latest
    steps:
      - run: free -m
      - uses: actions/checkout@v2
      - run: wget https://raw.githubusercontent.com/wtwver/atm/main/config/reconftw.bbrf?token=AM2642AZ5QKGJWXDYJB4SYTBLAAVY
      - run: wget https://raw.githubusercontent.com/wtwver/atm/main/config/notify.conf?token=AM2642AYJLAB7BRNM25DFDTBLAAUE
      - run: ls -R
      # - run: docker run projectdiscovery/nuclei -u ${{ secrets.HTTP }}://${{ secrets.DOMAIN }}
      # - run: docker run -m 4g wtwver/reconftw -d ${{ secrets.DOMAIN }} -s
      

name: sqlilab-sqlmap

on:
    schedule:
      - cron: '0 0 * * *'
    # push:
    workflow_dispatch:
      # inputs:
      #     target:
      #       required: false
      #       default: 'testfire.net'


jobs:
  nuclei-scan:
    runs-on: ubuntu-latest
    continue-on-error: true
    env:
      TARGET: ${{ github.event.inputs.target }}
    steps:
      # - run: echo "::add-mask::$TARGET"
      - run: curl ifconfig.me
      
      - run : docker run -d -p 2000:80 acgpiano/sqli-labs:latest
      - run: curl localhost:2000
      
      - run: docker run wtwver/sqlmap http://localhost:2000
      - run: docker run projectdiscovery/nuclei -u http://localhost:2000

